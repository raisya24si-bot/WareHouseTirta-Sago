@props(['action','placeholder','addAction','addText','filter'=>null,'statuses'=>collect()])
<div class="flex flex-wrap items-center justify-between gap-3 border-b border-outline-variant bg-surface-container-low/50 p-5">
<div class="flex flex-wrap items-center gap-2"><form method="GET" action="{{ $action }}"><div class="flex items-center overflow-hidden rounded-md border border-outline-variant bg-white"><span class="material-symbols-outlined px-3 text-outline">search</span><input name="search" value="{{ request('search') }}" placeholder="{{ $placeholder }}" class="w-64 border-none bg-transparent py-2 pl-0 pr-3 focus:ring-0"></div></form>@if($filter)<button type="button" onclick="document.getElementById('gudang-status-filter').classList.toggle('hidden')" class="inline-flex items-center gap-2 rounded-md border border-outline-variant px-4 py-2 font-label-bold hover:bg-white"><span class="material-symbols-outlined">filter_list</span>Filter</button><div id="gudang-status-filter" class="{{ request('status') ? '' : 'hidden' }} absolute z-20 mt-28 rounded-lg border border-outline-variant bg-white p-3 shadow-lg"><form method="GET" action="{{ $action }}" class="flex items-end gap-2">@if(request('search'))<input type="hidden" name="search" value="{{ request('search') }}">@endif<label class="text-sm font-semibold">Status<select name="status" class="ml-2 rounded-md border border-outline-variant px-3 py-2"><option value="">Semua</option>@foreach($statuses as $s)<option value="{{ $s->id_status_gudang }}" @selected(request('status')==$s->id_status_gudang)>{{ $s->nm_status_gudang }}</option>@endforeach</select></label><button class="rounded-md bg-primary px-4 py-2 text-white">Terapkan</button></form></div>@endif</div><button type="button" onclick="{{ $addAction }}" class="inline-flex items-center gap-2 rounded-md bg-primary px-5 py-2.5 font-label-bold text-on-primary hover:bg-primary-container"><span class="material-symbols-outlined">add</span>{{ $addText }}</button></div>
